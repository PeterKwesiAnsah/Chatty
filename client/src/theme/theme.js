import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
	palette: {
		common:{
			type:"light"    
		},
		primary: {
			main: '#D769E4',
		},
		secondary: {
			main: '#FF66BB',
		},
	},
});

export default theme;
